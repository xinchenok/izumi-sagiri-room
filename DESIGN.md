---
name: 和泉纱雾的房门里
description: 从半掩房门到晚安纸条的一次安静拜访
colors:
  strawberry: "#d8879f"
  strawberry-deep: "#a04f69"
  strawberry-pale: "#efbcc9"
  wallpaper-pink: "#d99aae"
  moon-silver: "#cfe8ed"
  moon-blue: "#3f6078"
  night-blue: "#273e59"
  night-deep: "#1c2c42"
  mint: "#a9cfbf"
  mint-pale: "#d9eee5"
  cocoa-door: "#573a3e"
  cocoa-mid: "#79545a"
  cocoa-light: "#a87672"
  cream-paper: "#fff6df"
  manuscript-paper: "#f8e8c5"
  paper-shadow: "#dfc99f"
  ink: "#3c2a31"
  ink-soft: "#684b56"
  quiet-white: "#fffaf1"
  butter-yellow: "#f3d16d"
  focus-blue: "#183f73"
typography:
  display:
    fontFamily: "Kuaile, YouYuan, Yuanti SC, sans-serif"
    fontSize: "clamp(2.4rem, 6vw, 5.6rem)"
    fontWeight: 400
    lineHeight: 1.16
    letterSpacing: "-0.04em"
  headline:
    fontFamily: "Kuaile, YouYuan, Yuanti SC, sans-serif"
    fontSize: "clamp(2rem, 4.3vw, 4.7rem)"
    fontWeight: 400
    lineHeight: 0.98
    letterSpacing: "normal"
  title:
    fontFamily: "Kuaile, YouYuan, Yuanti SC, sans-serif"
    fontSize: "clamp(1.55rem, 3vw, 2.5rem)"
    fontWeight: 400
    lineHeight: 1.16
    letterSpacing: "normal"
  body:
    fontFamily: "Microsoft YaHei, PingFang SC, Noto Sans CJK SC, Kuaile, Noto Sans JP Room, sans-serif"
    fontSize: "1rem"
    fontWeight: 400
    lineHeight: 1.72
    letterSpacing: "normal"
  label:
    fontFamily: "Microsoft YaHei, PingFang SC, Noto Sans CJK SC, sans-serif"
    fontSize: "0.72rem"
    fontWeight: 900
    lineHeight: 1.2
    letterSpacing: "0.08em"
rounded:
  sharp-paper: "0"
  pencil: "0.25rem 1.2rem 1.2rem 0.25rem"
  circle: "50%"
  pill: "999px"
spacing:
  compact-gap: "0.55rem"
  control-gap: "0.75rem"
  paper-pad: "1.2rem"
  content-gap: "2rem"
  section-block: "clamp(5rem, 10vw, 9rem)"
components:
  button-paw:
    backgroundColor: "{colors.strawberry-deep}"
    textColor: "{colors.quiet-white}"
    typography: "{typography.body}"
    rounded: "{rounded.pill}"
    padding: "0.65rem 1rem"
  button-pencil:
    backgroundColor: "{colors.mint-pale}"
    textColor: "{colors.ink}"
    typography: "{typography.body}"
    rounded: "{rounded.pencil}"
    padding: "0.55rem 0.9rem"
  button-round:
    backgroundColor: "{colors.cocoa-mid}"
    textColor: "{colors.quiet-white}"
    rounded: "{rounded.circle}"
    size: "3rem"
  nav-door:
    backgroundColor: "{colors.cocoa-door}"
    textColor: "{colors.quiet-white}"
    typography: "{typography.body}"
    rounded: "{rounded.sharp-paper}"
    padding: "0.55rem 1.1rem"
  paper-note:
    backgroundColor: "{colors.manuscript-paper}"
    textColor: "{colors.ink}"
    rounded: "{rounded.sharp-paper}"
    padding: "1.2rem"
  outfit-option:
    backgroundColor: "{colors.cream-paper}"
    textColor: "{colors.ink}"
    rounded: "{rounded.sharp-paper}"
    padding: "0.55rem 0.8rem"
  film-thumb:
    backgroundColor: "{colors.cream-paper}"
    textColor: "{colors.ink}"
    rounded: "{rounded.sharp-paper}"
    padding: "0.42rem 0.42rem 1.45rem"
  feedback-dock:
    backgroundColor: "{colors.night-deep}"
    textColor: "{colors.quiet-white}"
    rounded: "{rounded.sharp-paper}"
    padding: "0"
---

# Design System: 和泉纱雾的房门里

## Overview

**Creative North Star: “门缝里的安静拜访”**

这是一个 code-led 的房门偷看叙事：访客先用原生纵向滚动走过“房门—房间—桌面秘密—衣橱—画册—一起画—晚安”七幕连续镜头，画面在同一双层舞台中随靠近方式轻轻变化；愿意停下时，可以打开观察模式放大画面、触碰物件并主动听她回应。之后仍可进入原有房间、换装、画廊、深夜陪画和晚安纸条流程。它不是角色资料卡集合或地图关卡，而是一段有距离、有许可、会回应、会留下具体共同经历并真正结束的拜访过程。

草莓粉墙纸建立柔软私密的房间气氛，银蓝月光划分门外与夜晚，薄荷文具负责轻盈提示，可可木门承担结构与边界，奶油稿纸承载台词、留言和说明。可爱来自人物反应、创作物件和手工媒介，而不是廉价卡通堆叠；角色始终全年龄、日常化、非性感化。

品牌的五个不变量是：害羞但不冷淡，可爱但不幼稚，用创作细节讲角色，每次互动都轻柔而明确地回馈，人物始终处于安全日常情境。房门是本页的签名表达；后续界面可以复用“先观察、再获准靠近”的关系，不应机械复制同一扇门。

**身份、画风与构图三锁规则。** V19 七幕镜头以 `assets/v19/manifest.json` 指定的身份锚点锁定角色，以清晰画风锚点锁定线条、平涂、肤色与材质，再以各自旧图只锁定姿势、手势、道具、机位与叙事构图。可爱始终优先于写实华丽；不得把角色成熟化、网红化或性感化，也不得让旧参考中的脏色或纹理污染现役画风。

**人物场景清晰度规则。** 角色人物与房间场景必须使用清爽明亮的日系 TV 动画赛璐璐：平整无纹理色块、干净统一线条、最多两档硬边阴影、冷白银发、清透肤色与浅木家具。不得使用棕橙脏色、厚涂、水彩、油画笔触、颗粒、纸纹、噪点、污点、雾化、泛光或微细节堆积掩盖五官与动作；响应式导出只做 Lanczos 尺寸转换，不额外锐化、降噪或调色。只有明确作为“画中画”的小猫稿纸可以使用铅笔线条。现成插画已经画出窗框、月亮或人物轮廓时，页面不得再用伪元素重复覆盖。

**Key Characteristics:**

- 半掩木门、门牌和门把构成第一视觉阈值。
- 七幕原生滚动、双层画面与可选观察模式构成新的连续拜访入口。
- 猫爪印章、铅笔标签、胶带、拍立得、衣橱相册与胶片统一控件语言。
- 本地角色插画、操作触发的短拟音与语音承担证明内容，界面本身保持安静。
- 所有操作都汇入同一个反应与字幕停靠区，反馈不散落成多套浮层。
- 未完成稿纸、门口记忆纸角与晚安纸条只回显真实发生的片段，不把熟悉感换算成数值。
- 手作纸面保持清晰阅读顺序、键盘可达和 WCAG AA 对比目标。

## Colors

配色来自五种具体物件：草莓粉墙纸、银蓝月光、薄荷文具、可可木门与奶油稿纸。frontmatter 是唯一数值来源；正文只规定颜色关系和使用场景。

### Primary

- **草莓墙纸组：** `wallpaper-pink` 是站点背景，`strawberry-pale` 是房间与相册内页，`strawberry` 用于服装色样和纸屑，`strawberry-deep` 用于猫爪主行动、活动状态与印章。
- **可可木门组：** `cocoa-door` 是导航、画桌框架和门框的主深色，`cocoa-mid` 建立门板、相册边框和圆形翻页按钮，`cocoa-light` 表达被磨亮的木面层次。

### Secondary

- **银蓝月光组：** `night-deep`、`night-blue` 组成门外走廊、画稿暗室和反馈停靠区；`moon-blue` 是描边与晚安场景，`moon-silver` 是夜色上的正文和月光细节。
- **薄荷文具组：** `mint-pale` 是铅笔按钮和衣橱背景，`mint` 用于胶带、相片角和完成提示；薄荷只做平衡与工具提示，不抢走草莓粉的角色识别度。

### Tertiary

- **黄油亮点：** `butter-yellow` 只用于门把、猫爪进度、胶片选中框、夜间主按钮与庆祝纸屑，表达发现或完成。

### Neutral

- **奶油稿纸组：** `cream-paper` 是偏亮纸面，`manuscript-paper` 是台词、相册与晚安纸条，`paper-shadow` 是纸边与虚线压痕，`quiet-white` 是夜色上的温暖白。
- **墨色组：** `ink` 是纸面主文字，`ink-soft` 是说明文字，`focus-blue` 专用于高可见焦点轮廓。

**门内外规则。** 银蓝夜色只定义门外、画稿与晚安时刻；进入房间后必须由草莓粉、薄荷和稿纸接管，不把整页做成同一种粉色或同一种深色。

**黄油稀缺规则。** 黄油黄只标记可以被触碰、已经发现或值得庆祝的东西；同一局部不新增第二套高亮色。

## Typography

**Display Font:** 本地 ZCOOL KuaiLe 当前页面字符子集 WOFF2，以 `YouYuan`、`Yuanti SC` 和无衬线字体回退；原始 TTF 仅作为许可完整的维护源，不参与网页加载。
**Body Font:** 优先使用系统中文字体栈 `Microsoft YaHei`、`PingFang SC`、`Noto Sans CJK SC`；没有中文系统字体时以页面字符子集 `Kuaile` 兜底。日语字幕显式使用本地 `Noto Sans JP Room` 当前字符子集，避免无日文字体的设备出现缺字。

**Character:** Kuaile 像写在门牌、相册和纸条上的圆润手写标题；系统中文字体负责中文台词、说明、状态和版权信息；Noto Sans JP 只负责日语字幕和系统缺少日文字形时的回退。三者的关系是“有性格的短标题 + 克制易读的中文正文 + 稳定完整的日文字幕”。

### Hierarchy

- **Display：** Kuaile 常规字重、流式大标题，用于各场景二级标题；负字距只在这一层收紧。
- **Headline：** Kuaile 常规字重、紧凑行高，用于门牌角色名和相册核心名称。
- **Title：** Kuaile 常规字重，用于画稿标题、便签和局部互动主题。
- **Body：** 系统中文字体常规字重与宽松行高，用于叙述、台词与状态；说明段落通常控制在约 31–40rem。
- **Label：** 系统中文字体高字重、轻微字距，只用于门牌提示、场景名、进度和版权标签；不承担长段阅读。

**字体职责规则。** Kuaile 只负责短而有情绪的中文标题；日语字幕优先使用 Noto Sans JP；简体中文字幕、正文与交互状态优先使用系统中文字体。不得用装饰字体覆盖整页内容，也不得依赖访客设备恰好安装日文字体。

## Layout

页面使用纵向叙事与 1200px 最大内容宽度。桌面区段以左右非对称网格制造“门板遮挡、相册摊开、窗格并置”的空间感，不使用等宽卡片墙。区段纵向留白采用流式大间距，内容容器在宽屏由视口自动生成等量边距。

V19 七幕区域保持浏览器原生纵向滚动。桌面每个动作间隔约 58svh，手机约 48svh，每幕长度按帧间隔数计算。4:3 双层舞台粘在视口内，桌面稿纸靠右停留，手机稿纸停在画面下方，只显示当前幕说明。区段最后 14% 由门边、抽屉边或稿纸方向的裁切引入下一幕首帧；七枚进度纸签只作自愿跳转，不劫持滚轮或强制分页。

首屏的视觉与操作顺序固定为“门把 → 台词稿纸 → 轻轻敲门”：黄油色门把是视线锚点，门把下方的稿纸卡先解释门内状态与台词，猫爪按钮作为稿纸内的首要行动完成敲门。不得把敲门按钮移成脱离门体的全屏 CTA，也不得让台词先于门把抢占视觉中心。

- **1440px 校验画幅：** 内容最大宽度为 1200px，常规区段左右各约 120px；房门和相册主体最大约 1120px。首屏木门位于左侧前景，人物在右侧门缝出现，稿纸卡压在门板下部与人物交界处。
- **1024px 校验画幅：** 触发紧凑桌面布局；房门场景宽度为视口减 4rem，门板扩大到 57%，人物扩大到 78%。一张紧凑性格便签保持单列，画桌从左右分栏改为上下堆叠，衣橱相册调整为 55% / 45%。
- **390px 校验画幅：** 同时使用 760px 堆叠规则与 410px 窄屏修正；导航隐藏迷你门牌文字但保留五个 44px 纸签，房门场景高 40rem，稿纸卡占 74% 并贴近门把下方。主要区段单列，补充插画保持原始 4:3 比例，五秘密热点以物件微光给出短暂线索、发现后留下细小痕迹，命中区不缩小；隐藏留言使用小头像与完整正文两列，衣橱照片高 28rem，胶片横向滚动，“今晚别走太早”按“标题—人物与藏画—稿纸选择—停留节点或成稿”堆叠，反馈停靠区贴齐左右安全边距。非语音互动只浮出最多两行的紧凑反应并可主动收起；语音控制仅在声音情境中随字幕展开。
- **七幕观察画幅：** 普通舞台在手机读取 720 档、桌面读取 1440 档；只有访客主动进入观察对话框时，手机才读取 1440 档，桌面才读取 4K。观察画面最大放大 200%，平移必须夹在可视边界内。
- **矮桌面窗口：** 761px 以上视口不再无限压缩房门舞台；场景最低保持 680px 高，让门牌、门把与敲门稿纸维持分区，空间不足时由页面自然纵向滚动。

**叙事顺序规则。** 响应式只能重排空间，不能改变“门口—连续房间—画桌秘密—衣橱—画稿—今晚别走太早—晚安”的阅读顺序。深夜陪画在看过生活物件与画稿后承担情绪高潮，但允许访客只停留而不完成；晚安只在它之后收束，连续房间内部四个位置随时可达，不计入章节解锁顺序。

## Elevation & Depth

层次由真实物件关系、透视门板、纸面重叠和少量环境阴影共同建立。门外和画稿使用更深的夜色阴影，便签和相片使用粉棕阴影；没有玻璃拟态、模糊背板或纯黑硬阴影。

### Shadow Vocabulary

- **门槛深度：** 房门舞台使用大范围夜色投影并配合内描边，表达门框厚度与前后景距离。
- **纸面浮起：** 便签、拍立得、相册内页使用低不透明粉棕阴影，配合轻微旋转而不是额外容器。
- **夜间画框：** 画稿主图与窗格使用更深、更宽的环境阴影，在深蓝背景上仍保持边缘可辨。
- **反馈停靠：** `feedback-dock` 使用单层紧凑阴影；临时浮起时不增加第二层更重的阴影。

**物件先于阴影规则。** 先用门框、相册书脊、窗格、纸边和遮挡建立深度，阴影只补充环境光；任何看起来像黑色描边的投影都过重。

## Shapes

形状语言以真实房间物件为准：门框、稿纸、相册、胶片和导航保持明确直角；猫爪主按钮是胶囊，翻页与印章是圆形，铅笔按钮使用一端方、一端圆的削笔轮廓。纸面可用轻微旋转、胶带、撕边和 `clip-path` 制造手作感，但正文基线与点击区域保持稳定。

- **门牌：** 直角稿纸、双薄荷挂点、圆形猫爪印章。
- **猫爪：** 胶囊按钮与爪印进度共同表达主要行动和发现状态。
- **铅笔：** 左端短直角、右端圆角，负责语音等次级行动。
- **拍立得 / 衣橱相册：** 奶油纸相框、薄荷照片角、可可书脊和轻微页转透视。
- **胶片：** 直角缩略图、底部双齿孔、横向卷轴和黄油色选中框。

**轮廓归因规则。** 每个可见形状都必须能被解释为门、纸、胶带、印章、铅笔、照片、相册或胶片；不得加入没有叙事来源的通用圆角卡片。

## Components

### 七幕连续镜头与观察模式

七幕按照房门、房间、桌面秘密、衣橱、画册、一起画、晚安排列，共使用 26 个独立关键帧。连续舞台只由原生滚动位置驱动：双图片层交叉控制透明度，镜头以各幕登记的桌面 / 手机机位轻推；当前幕标题、短句、帧位置和七枚纸签同步更新。图片载入必须经过解码缓存与递增序列号，快速反向滚动时旧请求不得覆盖新镜头；Save-Data、2G 或 slow-2G 下停止邻帧与下一幕预取。

“看看房间”打开原生 `dialog`，展示当前幕专门登记的稳定观察帧；该帧必须同时看得到本幕三个热点物件。图片使用 `contain` 完整显示，热点位置按实际渲染图片矩形换算，竖图不能因宽屏裁切而永久丢失门把等物件。鼠标支持拖动、滚轮和双击，触摸支持拖动与双指缩放，键盘支持方向键、加减键和 Home 复位；关闭后恢复原滚动位置与触发按钮焦点。点击热点时暂时隐藏热点并切到反应帧，约一秒后回到稳定观察帧，避免按钮落在反应帧里不存在的物件上。每幕三个物件热点与一个角色热点使用原生按钮，加载失败时保留当前幕文字、重试按钮和退出路径。

### 动效模式

顶部安静灯在 `lively` 与 `quiet` 间切换并保存到 `sagiri-room-state-v2.motionMode`。安静模式停用连续混合、自动反应、雨景与视差，但保留当前静态帧、七幕文字、观察操作和主动声音；系统 `prefers-reduced-motion` 启用时覆盖站内偏好、禁用切换按钮并具有最高优先级。

### Navigation

顶部导航是粘在门框上的可可木条：左侧迷你门牌，中间五张略有手工错落的奶油 / 薄荷纸签，右侧“请小声一点”状态灯。当前区段使用带小猫爪印的黄油纸签，悬停和键盘焦点保留黄油下划线；移动端隐藏状态灯并压缩门牌，但五张纸签仍保留 2.75rem（44px）最小触控尺寸。页内移动由脚本接管，只改变滚动位置而不写入 `#living-room` 一类地址片段。访客仍在门外时点击任一室内纸签，脚本必须先完成同一套敲门回应和开门时序，再滚动到目标区段，不能越过房门许可。全新访问保持房门首屏，浏览器前进、后退与刷新沿用原生滚动恢复；只有携带旧地址片段的入口会先清理片段并回到顶部，避免浏览器锚点和脚本争抢位置。

### 房门首屏

房门舞台由银蓝走廊、草莓墙纸内景、响应式人物 WebP、透视门板、门牌、门把和稿纸卡叠成。初始状态只能偷看；敲门后人物先受惊，再在默认 760ms 后脸红并开门，门板以 1100ms 绕左侧旋转。桌面布局开门后，人物插画扩展至整个门框，前景门板在旋转中淡出，由插画内的木门自然接管构图，不能留下粉色竖条或残破门牌。门牌承担身份，门把承担视觉引导，稿纸承担台词，猫爪按钮承担许可动作，四者不可互相替代。

房门同时承担回访关系变化：本地状态只在跨日期或间隔至少四小时后记为一次新拜访，并依次使用“第一次站在门外”“认得脚步声”“提前留出门缝”三组门牌、门后短句、开门回应和抽屉留言。关系变化不显示次数，不锁内容，也不使用签到、经验值或好感度条；刷新页面不能反复推进阶段。

已有记忆时，门口稿纸卡内出现一张薄荷纸角；它比较已收纸条、共同成稿、十二个房间事件与成功换装各自的真实时间，只展示最新一项。点击纸角只把该项具体记忆写入原有门口台词与统一反馈区，不替换敲门行动；没有真实记录时保持隐藏。

### 会生活的房间

开门后的第一个完整体验是一扇 4:3 房间取景框和一条可可木滑轨。画桌、床边、衣橱、窗台不是四张功能卡，也不是地图关卡：切换地点时沿用同一取景框、稿纸和木轨，画面、地点短句、可触碰小事、专属角色语音、短场景声与深入入口一起改变。桌面为“房间画面 + 从右侧压住画面的奶油稿纸”，手机按“画面—稿纸—横向木轨”堆叠；稿纸不能遮住人物脸部，木轨当前地点用黄油纸签与位置共同表达。

四个地点分别使用画桌落笔、床边被角、衣橱衣架和窗台窗帘作为签名动作。地点进入视口并停留约 5.6–9.2 秒后，才出现一次不打断阅读的自主细节：眨眼换帧或笔尖、布料、衣架、窗帘的低幅动作；它不弹反馈停靠区，也不推进任何记录。每个地点另有一组可重复的小事件，连续点击按小循环更换台词，不计数、不奖励、不锁内容。

只有访客实际触发十二个地点事件之一时，才把地点、事件序号与发生时间写入最近房间记忆；自主细节、仅切换地点或等待不会伪造一段共同经历。该记录立即参与门口纸角与本次晚安纸条的内容选择。

本地时间只选择清晨、午后、傍晚、夜里或深夜的灯光与短句；“房间里的天气”是访客可以自由切换的细雨 / 晴月氛围，必须明确不是联网真实天气。天气不限制地点或内容。桌边拍立得只在已有 `sharedDrawing` 时出现，并直接使用上次共同完成的主题与配色；记忆表现为房间物件，不显示完成次数。

细雨只能画在插画中能明确辨认的室外玻璃窗格内。每个地点以原图像素坐标保存独立多边形窗格和随机种子，多边形必须沿窗帘、人物头发、灯具遮挡与倾斜窗框内缩；Canvas 结合实际 `object-fit: cover` 尺寸重新映射，并对每一块玻璃分别裁剪。窗框必须切断雨痕，床边完全隐藏；切到晴月时连续房间和共同创作两张 Canvas 都必须清空。雨滴使用不同速度、长度、弧度、双层明暗拖痕和少量水珠，不得用整段背景斜纹、近似矩形或等距 CSS 线铺满室内，也不能压到人物、窗帘、窗框、灯具、床、桌面或地板；看不清室外玻璃的画面宁可不显示。

每个地点都有一条开场语音和三条事件语音，雨起与雨停另各有一条；共十八条 `assets/audio/v8/voice` 专属 MP3，不读取 `CONTENT.voiceLines`，也不与主线二十二句复用。每条回答对象同时绑定场景、标签、日文、简体中文字幕、反应和文件路径。十二个地点事件使用 `assets/audio/v14` 中十二份不同来源的 CC0 现场录音：数位笔、平板滑动、按键、被毯、床单、靠枕、衣架、布料、选衣、关窗、画纸与窗帘必须逐项对应，不使用程序化噪声或跨事件复用。两段天气声继续位于 `assets/audio/v8`；雨起使用真实室内窗玻璃录音，窗台三次事件不固定混入雨声，避免晴月状态仍然听见雨。全部声音只在对应操作后播放、不循环、不自动播放；运行文件、来源页、取得哈希、处理参数与许可必须在本地清单中对应。

音频只在访客按下控件、敲门或主动播放时准备，不再由视口观察或纯滚动请求。文件使用低网络优先级并转换为本页独立 Blob 源；每个文件保留自己的播放器。点击时若内存源已就绪直接播放，尚未就绪先显示“正在准备”，并由原始本地文件承接播放；失败后下一次点击可以重试。观察弹窗内有独立可见的日文、中文字幕、播放状态和两类声音开关，切物件声音不能改掉正在播放的台词。

“场景声”开关只控制十二段物件声、两段天气声和一段晚安关门声，不能静音角色语音；“只看角色字幕”只控制角色语音，不能关闭场景声。任一音频失败时仍保留准确的双语字幕、地点反应和视觉状态。房间位置、天气和场景声开关写入既有 `sagiri-room-state-v2`，旧状态缺少字段时回落到画桌、细雨和开启场景声。

### Buttons

- **猫爪主按钮：** 草莓深色胶囊、温暖白字、最小高度 3rem；夜间变体改用黄油黄与墨色。
- **铅笔按钮：** 薄荷纸底、银蓝描边、不对称圆角、最小高度 2.9rem，用于语音和次级动作。
- **圆形翻页按钮：** 可可中间色圆形、3rem 方形点击区，用于相册与画稿前后切换。
- **States：** 悬停上移 2px，按下下移 2px；所有按钮与链接使用 3px `focus-blue` 可见焦点并外偏移 4px。新增触控控件的可用命中区不得小于 44 × 44px；紧凑图标的可见圆可以更小，但必须补足外围命中区。

### 稿纸、相册与胶片

性格说明收束为一张带胶带与手写引用的紧凑稿纸，只保留能够解释房间物件和互动反应的内容，不用三张大卡重复人物资料。衣橱是左右摊开的实体相册，五套服装切换同时更新响应式 WebP、名称、描述、时间、`aria-pressed` 和反应；只有图片准备成功并真正换到另一套服装后，才记录服装与发生时间供门口纸角和晚安纸条引用。新增“草莓睡前服”强调困倦仍画画的状态，“猫耳连帽毯”强调怕生、家里蹲与截稿时认真的性格，不做性感化睡衣或通用时装展示。

画册固定为十一个不重复情景，按“第一章 · 偷偷开始”“第二章 · 把画推近”“第三章 · 等一句夸奖”组织，章节只负责叙事定位，不锁定内容。胶片条横向滚动并用黄油边框与位置变化共同表示选中；点击主图进入受焦点保护的暗房，支持上一张 / 下一张、键盘方向键、触屏水平滑动和点击原图放大，关闭后回到原画稿位置。页面只预取当前画稿相邻两张。“夸夸她的画”语音入口紧跟胶片控件，以一张贴了胶带的小纸条承接“看画 → 夸她 → 听回答”，不得移回无关区段。

### 五个小秘密画桌

画桌使用一张 4:3 本地场景插画承载真实空间：纱雾坐在画桌后，耳机、轻小说稿件、数位板、猫咪玩偶与带锁抽屉分别占据互不重叠的热点区域。说明区与画面共用奶油稿纸和可可木框，不在插画上再叠大块通用矩形。热点位置按插画百分比锚定，命中区保持 44–52px；等待一会儿或连续两次点空时，只让下一件物件出现与材质相符的短暂微光（屏幕反光、耳机圈、折角、玩偶呼吸或钥匙亮点），不叠通用标记。发现后留下细小薄荷猫爪，帮助访客记住已经看过的位置；文字线索只描述物件方向，不提供“提示”按钮，也不自动替访客点击。

### 今晚别走太早 · 深夜陪画

“看过那些画以后，今晚别走太早。”位于画稿之后、晚安之前，是整次拜访的情绪高潮：访客已经看过她的生活和作品，才被允许靠近仍未完成的画。它既可以收束为一张共同成稿，也允许访客不说话地留下；桌面使用“左侧连续人物舞台 + 右侧横线稿纸”非对称双页构图，移动端按“标题—人物与藏画—稿纸选择—停留节点或成稿”堆叠。深蓝雨夜只作为工作室边界，奶油稿纸始终承担问题、选项、停留回应与结果。

流程为“允许靠近—选择陪伴方式—选择小猫主题—选择配色—给出具体感想—她把完成稿推过来”。点击开始便从第 0 步把已选内容、当前步骤、普通 / 安静模式、安静累计、偷看次数与更新时间写入 `sagiri-room-state-v2.drawingDraft`；每次选择、返回、偷看和安静停留都会继续同步。刷新后开场稿纸同时提供“接着上次没画完的地方”与“换一张空白稿纸”；完成时才写入 `sharedDrawing` 并清除草稿。

选择“安静坐在旁边”后，稿纸显示 20、45、75 秒三个可见停留节点和一枚随时继续创作的铅笔按钮。累计只在页面处于前台且本区段可见时前进；页面隐藏、离开区段或离开页面时先保存再暂停，回到这里后从原处继续。75 秒节点仍使用害羞帧并把选择权轻轻交回来，不切到成稿完成帧，也不发放奖励、计数或内容解锁。

选定小猫主题后，人物舞台内出现“偷看画到哪里了”。每次触发都让稿纸先滑入露画、再盖住画面、最后害羞地只露出一角；前三次分别使用受惊、害羞与稍微推近的简体中文反应，继续触发时维持第三种边界。该反馈只更新视觉、舞台说明与统一反馈区，不调用既有日语语音，也不把旧音频字幕挪来冒充新内容。

主题选择立即出现草稿预览，配色以草莓、薄荷、银蓝三种纸面染色表达，不能把整张画套高饱和滤镜。舞台人物只使用四个既有叙事帧：专注、自然眨眼、被认真夸奖后的害羞、把成稿推近的完成帧；眨眼仅在区段可见、页面前台、人物处于专注帧且没有交互忙碌时随机发生。雨滴仅落在右上玻璃多边形内，并严格跟随连续房间的细雨 / 晴月状态，台灯呼吸和笔尖微光只做低幅环境动作。所有选项是原生按钮，键盘确认后焦点移动到下一步第一个选项，返回按钮允许修改上一步，完成后焦点落到“再陪她画另一张”。

**自愿停留规则。** 三个时间节点只证明角色反应随共同安静发生变化；它们不是倒计时、任务进度或换取成稿的门槛，访客在任何节点都可以继续画或离开。

### 晚安与关门

晚安不是又一张可无限点击的随机卡，而是拜访的终点。默认纸条先引用本次拜访实际完成的画、触发的房间事件或成功换装：只有共同成稿与房间事件都发生在本次拜访时才组合两者，否则依次使用本次成稿、本次房间事件或本次换装；没有本次事实时才按真实时间回看最近一项记录，不跨拜访拼成没有发生过的共同经历。访客仍可以换一张纸条、听一句晚安或复制文字；主要行动“收下纸条，轻轻带上门”会把纸条内容与时间写入现有状态，停止正在播放的角色语音，播放一段独立的真实木门轻合声，让门板从打开状态完整闭合，并把页面送回房门。下一次打开页面会重新站在门外，但晚安区仍显示上次收下的纸条，形成可被记住而不量化的关系痕迹。关门声遵循场景声开关，只在主要行动后播放一次，不循环、不自动播放。

**真实记忆规则。** 门口纸角与晚安纸条都从带时间的实际记录取材；组合文案只能使用同一次拜访中共同发生的事实，不能为了显得亲近而补写不存在的互动。

### Feedback Dock

`feedback-dock` 是全站唯一反馈容器，把左侧即时表情与右侧日语 / 简体中文字幕合并为一条连续停靠带。静态时它位于主要内容之后、页脚之前，宽度上限 72rem；任意敲门、换装、秘密、画稿、纸条或语音操作触发后，它临时固定浮到视口底部，正文同时留出底部空间。非语音互动会把中文字幕区改成本次反应，清空日文行，并在移动端隐藏声音控制、把文字限制为两行以保持紧凑；V17 的开始陪画、恢复草稿、偷看藏画和安静停留反馈都遵循这条简体中文路径，不能启动或借用旧日语音频。右上角的下收按钮可以立即结束本次浮起，不必等待自动消失。只有播放语音或主动切换声音模式时才展开音量、停止和“只看角色字幕”。没有暂停、播放进度条、自动播放或通用循环环境音。

普通语音回答按入口各自洗牌，一轮听完以前不重复，重新洗牌时也避免与上一轮末句相邻重复；四句 `ふきげん` 回答独立放在门口和画桌的情境池，仅当同一入口 5 秒内连续触发 4 次时播放，随后冷却 25 秒，不得混入普通随机。回答同时更新场景标签、人物表情、日语字幕和简体中文字幕。音量与只看角色字幕偏好保存在现有浏览器状态中；静音后再次触发仍完整展示字幕和表情，不创建系统 TTS 回退。

### Interaction & Motion

敲门使用“受惊—脸红—开门”，七幕连续镜头使用双层透明度混合、低幅镜头推移、4–7 秒后最多三次的自主反应与物件边缘提示；连续房间、深夜陪画、换装、画稿、秘密、晚安和反馈仍沿用各自既有动作。`CinematicDirector` 用 `IntersectionObserver` 判断整段是否可见，被动 `scroll` / `resize` 监听只标记脏状态，实际测量与呈现合并到 `requestAnimationFrame`；图片加载与自主帧分别使用递增序列号取消过时结果。所有人物换帧只能使用透明度、轻微缩放与裁切，不得施加 `blur()`。

滚动、自动换帧和页面加载永远不播放声音。只有观察对话框中的物件热点或角色热点被访客主动触发时，才允许播放对应语音与拟音并更新统一字幕 / 反应停靠区。状态持久化继续保存原有服装、秘密、回访、共同画稿、房间、声音和纸条字段，并新增 `motionMode`；图片失败保留文字、重试和退出路径，音频失败保留字幕与视觉反馈。

在 `prefers-reduced-motion: reduce` 下，平滑滚动关闭，七幕固定在每幕首帧，双层混合、镜头推移、自主反应、雨景、纸屑、随机眨眼、房间物件动作、视差和长过渡全部停止；系统偏好覆盖保存的 `motionMode`。减少动态只改变表现，不删除七幕、观察对话框、热点、内容、地点、状态、字幕或主动反馈。

### Accessibility & Local Assets

以 WCAG AA 为最低目标：原生按钮支持键盘，页面提供跳转主要内容链接，焦点轮廓不依赖颜色状态之外的隐含信息；选项同时使用位置 / 边框与 `aria-pressed`，进度和字幕使用 `aria-live`，图片有简体中文替代文本，日语语音始终伴随简体中文字幕。新增互动遵循 44 × 44px 最小触控命中区。

`assets/v19` 是七幕连续镜头的现役运行目录。26 张独立母版包含 8 张 3072 × 3840 竖版人物与 18 张 3840 × 2880 的 4:3 场景；连同竖版 720 / 1440 与场景 480 / 720 / 1440 派生图，共有 96 个 WebP。`assets/v19/manifest.json` 记录身份 / 画风锚点、对应旧图、尺寸、文件大小、SHA-256、提示词和 provenance 边车；可读提示词位于 `assets/v19/prompts`。

V19 初次生成的一整批新图因棕橙、颗粒、油画与脏感被用户否决后，完整隔离在非运行时临时目录，没有进入现役资产；最终批次以主 agent 的干净校准图重新逐张编辑，再用 Pillow Lanczos 纯尺寸转换导出母版和派生图，不添加锐化、纹理、降噪或调色滤镜。完整过程与运行合同见 `docs/v19-cinematic-room.md`。

V19 不删除、覆盖或移动 V18.1 及更早图片；原有页面区段仍可继续引用自己的 V18 资源，七幕切换前的 107 个 V18.1 WebP 则由 `assets/archive/pre-v19-cinematic-images.json` 保存路径、尺寸、文件大小与 SHA-256。V18 / V18.1 的生成与早期归档继续以 `docs/v18-4k-image-redraw.md` 和 `assets/v18/manifest.json` 为历史权威。

七幕使用 `assets/audio/v19/voice` 的二十八句 Piper Plus「つくよみちゃん」日语语音与 `assets/audio/v19/foley` 的二十一段不同来源 Freesound CC0 实录；每幕三个物件热点各自绑定一段拟音与一句语音，角色热点只触发该幕角色语音。模型 revision、哈希、零说话人嵌入、逐句查询和验证见 `docs/v19-voice-reproduction.md` 与 `assets/audio/v19/voice-manifest.json`；拟音来源见 `assets/audio/v19/CINEMATIC-FOLEY-SOURCES.md`。旧房间与主线语音继续按原入口使用，相关许可原文与页脚来源不得删除。不得改为远程热链，也不得使用官方截图、动画音轨、真实声优声纹克隆或后期移调。

旧房间继续使用 `assets/audio/v8/voice` 的十八句专属语音、`assets/audio/v14` 的十二段真实事件拟音、`assets/audio/v8` 的两段天气声和 `assets/audio/v16` 的晚安关门声；主线二十二句 AivisSpeech 回答继续位于 `assets/audio/v4`。页脚必须长期保留 AivisSpeech「天深シノ」、Piper Plus「つくよみちゃん」的来源 / 条款、旧拟音与 V19 二十一份 CC0 实录的来源入口，以及 ZCOOL KuaiLe 与 Noto Sans JP 依据 SIL Open Font License 1.1 使用的说明。

## Do's and Don'ts

### Do:

- **Do** 把访问许可写进构图与交互：先看见门和门把，再读台词，最后敲门。
- **Do** 让画桌、床边、衣橱和窗台共享同一取景框与木轨，并让每个地点都有自主细节、可重复小事件和明确的深入入口。
- **Do** 使用草莓墙纸、银蓝月光、薄荷文具、可可木门和奶油稿纸建立明确物件关系。
- **Do** 让猫爪、铅笔、胶带、拍立得、衣橱相册和胶片承担控件语义。
- **Do** 让每次点击都有表情、字幕、状态或动效反馈，并统一进入 `feedback-dock`。
- **Do** 用用户刚刚选择的主题、配色和具体感想写回后续台词，让共同经历可被自然记住。
- **Do** 从第 0 步保存未完成稿纸，让刷新后的访客可以接着画或主动换一张空白稿纸。
- **Do** 让 20、45、75 秒停留节点只在页面前台且陪画区可见时累计，并始终保留“继续画”与离开的自由。
- **Do** 按实际时间从纸条、成稿、房间事件与成功换装中选择最新记忆，只组合本次拜访里确实一起发生的内容。
- **Do** 保持角色造型全年龄、日常化、非性感化，并让本地素材署名长期可见。
- **Do** 让每张新插画同时服从统一身份锚点和各自旧图构图，以可爱、害羞和角色可辨认性作为首要验收条件。
- **Do** 让七幕只响应原生滚动，并在观察模式中为鼠标、触摸和键盘提供等价的放大、平移、热点与退出路径。
- **Do** 用平整无纹理色块、最多两档干净阴影、冷白银发、清透肤色与浅木家具维持 V19 清晰画风。
- **Do** 在 1440px、1024px 与 390px 校验叙事顺序、44px 触控和双语字幕完整性。

### Don't:

- **Don't** 使用霓虹电竞感、紫色科技渐变、装饰性玻璃拟态或纯黑硬阴影。
- **Don't** 把页面改成左右双栏营销首屏、统一尺寸动漫卡片墙或脱离门体的全屏 CTA。
- **Don't** 把连续房间做成地图关卡、四张地点卡、限时天气、地点解锁或探索进度。
- **Don't** 用通用圆角容器稀释门牌、稿纸、相册与胶片的真实轮廓。
- **Don't** 让临时反馈散落成多个 toast、表情角标和字幕条；只扩展统一停靠区。
- **Don't** 让动效遮挡内容或忽略减少动态设置，也不要只用颜色表达选择与完成。
- **Don't** 把共同创作改成答题、闯关、签到、积分、好感度、奖励货币或内容解锁。
- **Don't** 把 20、45、75 秒写成倒计时、奖励条件或完成进度；75 秒仍是害羞停留，不是完成帧。
- **Don't** 用跨拜访拼接、等待时长或自主动画虚构一段共同记忆。
- **Don't** 给 V17 的简体中文新反馈套用旧日语音频或伪造对应字幕。
- **Don't** 把角色重绘成熟化或网红化，也不要用单纯放大旧像素代替真正重新绘制。
- **Don't** 因滚动、自动换帧、预取或页面加载播放声音；声音必须来自观察模式中的明确主动操作。
- **Don't** 让保存的“活泼”模式覆盖系统减少动态效果，也不要在 Save-Data 下继续预取相邻大图。
- **Don't** 使用官方图片、动画音轨、声优声纹克隆、远程字体或未署名的本地语音；也不要跨房间地点或事件复用同一条语音来制造“内容变化”。
