# AI 协作开发与维护手册

- 更新日期：2026-08-14
- 项目仓库：<https://github.com/xinchenok/izumi-sagiri-room>
- 在线页面：<https://xinchenok.github.io/izumi-sagiri-room/>
- 当前用户可见功能基线：[`d1a08d9`](https://github.com/xinchenok/izumi-sagiri-room/commit/d1a08d949cd6545ca8c84a9b42886ade29bcb599)
- 当前页面缓存标识：`20260814-1`

本文记录“和泉纱雾的房门里”互动页如何在 AI 协助下完成、语音资源从哪里取得、怎样在本地重新合成，以及后续维护计划。它既是开发说明，也是后续接手者的复现入口。

## 先说明：本项目没有训练语音模型

本项目没有收集训练数据，没有训练、微调或克隆任何声优声纹，也没有截取动画音轨。主线语音采用 AivisSpeech 预训练模型“天深シノ”，在本地通过 AivisSpeech Engine 独立合成二十二句日语回答；房间 V8 另用 Piper Plus 预训练模型“つくよみちゃん”生成十八句地点 / 事件 / 天气专属回答。两组都使用独立文本和独立文件，不把同一句台词变速、变调后当成新回答。

因此，准确说法是“使用预训练模型进行本地语音合成”，不是“训练了一个和原作声音相同的模型”。页面只追求害羞、轻声、开心和稍微不高兴等情绪方向，不冒充原作声优，也不宣称与官方声音相同。

## AI 协助完成了什么

AI 在人工给定角色方向、功能目标和版权边界后，协助完成了以下工作：

1. 把页面整理为“门口初见 → 连续房间 → 画桌秘密 → 雨夜一起画 → 日常换装 → 秘密画稿 → 晚安告别”的房门偷看叙事。
2. 建立草莓粉、银蓝、薄荷青、可可棕和奶油纸色组成的视觉系统，并记录在 `DESIGN.md`。
3. 生成一张角色身份锚点，再基于同一身份制作四张首屏表情、五张换装图、十一张运行时画册情景图、一张五秘密画桌场景图、四张雨夜共同创作人物帧、三张小猫成稿和一张 V7 横向衣橱场景；V7 其余地点复用已经验收的同世界画面。
4. 使用原生 HTML、CSS 和 JavaScript 实现敲门、三阶段回访文案、表情切换、连续房间、无常驻标记彩蛋、雨夜共同创作、换装相册、章节画廊与暗房、语音字幕和控制、晚安纸条和统一反应角。
5. 将人物表情、服装、台词、反应、画廊和彩蛋集中在 `script.js` 的 `CONTENT` 对象中，避免内容散落在事件处理代码里。
6. 在 `sagiri-room-state-v2` 中扩展回访次数、上次有效拜访、共同完成的画、连续房间位置与天气、场景声开关、画册位置、音量和只看角色字幕偏好，并保留从 `sagiri-room-state-v1` 迁移服装和彩蛋记录的逻辑。
7. 使用 AivisSpeech 在本地生成二十二句主线回答；十八句轻声、普通或开心回答按入口洗牌并保证一轮听完前不重复，四句 `ふきげん` 回答只在访客短时间连续催门或催稿时使用。房间另用 Piper Plus 生成十八句不复用主线内容的专属回答，并为十二个地点事件和两个天气状态制作十四段独立场景声。
8. 将运行时人物与换装切换为完整尺寸 / 560px 两档高质量 WebP，共同创作与 V7 衣橱场景使用 1448 × 1086 / 720 × 540 两档 WebP；将 ZCOOL KuaiLe 与 Noto Sans JP 都缩成当前页面字符子集 WOFF2，并取消画册、衣橱与连续房间的一次性全量预取。
9. 建立 2048、1440、1024 和 390px 的发布前浏览器清单，并对键盘操作、手机横滑、房间自主变化、暗房、回访状态、只看角色字幕、独立场景声开关、减少动态效果、音频失败降级、资源引用和 UTF-8 编码进行自动检查。
10. 把固定 CSS 雨线升级为逐场景、逐窗格裁剪的 Canvas 玻璃雨滴，并用确定性多层 DSP 重制 7.224 秒高质量立体声窗雨。
11. 将静态站点提交到 GitHub，并使用 GitHub Pages 从 `main` 分支根目录发布。

AI 生成的内容经过人工目标约束与浏览器验收；AI 不是素材版权、许可兼容性或上线质量的最终责任主体。

## 完整实施与修复记录

本节是项目截至 2026-08-13 的交付账本。带提交链接的发布表记录已经进入 GitHub `main` 并由 GitHub Pages 提供的实际工作；未完成的规划仍只放在“已知问题与后续规划”，不会混写成已交付功能。

### 页面、角色与交互

- 从零建立“门外轻敲—获得回应—进入房间—探索画桌—翻衣橱与画册—听晚安”的完整单页叙事，并保持零框架、无后端、无运行时外部依赖。
- 根据和泉纱雾“怕生、重度家里蹲、认真画画、被夸时会嘴硬和害羞”的角色锚点统一文案与场景；所有服装和画面坚持全年龄、日常化、非性感化，不复制官方截图。
- 首屏提供偷看、受惊、害羞和认真得意四种表情。访客敲门后先出现受惊过渡，再打开房门；门板、人物图和状态文案使用同一演出状态，不会出现两个互相冲突的开门结果。
- 加入初次拜访、逐渐熟悉和主动留门缝三个回访阶段，根据有效拜访次数与间隔更新门牌、开门反应和隐藏留言，不显示签到、经验值或好感度数值。
- 建立统一反应停靠区，让敲门、换装、秘密、画册、晚安和语音都在同一处更新人物表情、场景标签与反馈，避免多套浮层互相遮挡。

### 雨夜共同创作与具体记忆

- 在画桌与衣橱之间加入“今晚，陪她画完这一张”：访客依次选择陪伴方式、小猫主题、草莓 / 薄荷 / 银蓝配色和一句具体感想；所有选择都可直接使用、可返回修改、可重新画，没有正确答案或失败状态。
- 制作专注、自然眨眼、被认真夸奖后的害羞、把完成稿推近四个连续人物帧，以及“门缝看月亮”“被窝里画星星”“抱着大铅笔”三张奶油稿纸小画；舞台只在进入视口后加载眨眼帧，害羞与完成帧只在开始互动后预取。
- 以雨线、台灯呼吸、笔尖微光和 145ms 自然眨眼让画室有生命；动画只在章节可见、页面前台、人物空闲时运行。减少动态效果下停止随机眨眼并隐藏雨线与笔尖微光。
- 将 `presence`、`subject`、`palette`、`praise` 和完成时间写入现有 `sagiri-room-state-v2.sharedDrawing`；旧状态没有该字段时保持兼容。再次回来时，桌边会出现上次成稿，并自然复述陪伴方式、配色和具体感想。
- 选项均为原生按钮，支持 Enter / Space、可见焦点、44px 触控与 `aria-pressed`；步骤变化由 `aria-live` 宣布，完成后焦点落到“再陪她画另一张”，图片加载失败仍保留文字与选择流程。
- 该章节不加入签到、打卡、积分、好感度、经验值、成就、抽卡、排行榜、奖励货币或内容锁；停留价值来自共同创作、角色反应和可见作品，而不是机械留存系统。

### V7 会生活的连续房间

- 在开门后的房间介绍中加入统一 4:3 取景框和可可木滑轨，让访客在画桌、床边、衣橱和窗台之间移动；桌面使用“房间画面 + 右侧重叠稿纸”，手机按“画面—稿纸—横向木轨”堆叠，不把地点做成四张卡片或地图关卡。
- 画桌复用 V6 专注 / 眨眼帧，床边与窗台复用 v4 已验收画册场景；只为缺少横向机位的衣橱生成一张 1448 × 1086 新图和 720 × 540 移动版，背景同时保留床、窗与画桌以证明仍是同一间房。
- 每个地点都提供一组可重复的小事件和三条轮换反馈：补完一笔、递回毯角、扶稳衣架、关小窗缝；重复操作会变化台词和局部画面，但不计数、不奖励、不锁内容。地点另有停留约 5.6–9.2 秒后出现的自主细节，不弹任务或提示。
- 本地时间选择清晨、午后、傍晚、夜里和深夜的灯光与短句；细雨 / 晴月明确标记为“房间里的天气”，由访客随时切换，不联网、不冒充真实气象、不制造限时内容。
- 使用 FFmpeg 噪声源与包络制作笔尖、布料、衣架、窗帘 / 雨声四段 0.94–1.38 秒本地短拟音；只在用户操作后播放，不自动播放、不循环，并使用独立“房间声”开关。最终页面播放峰值约 -20～-23 dB，手机扬声器可感知但不会压过角色语音。
- 将 `livingPlace`、`livingWeather` 与 `roomSoundMuted` 写入现有 `sagiri-room-state-v2`；旧 V6 状态没有字段时回落到画桌、细雨和开启拟音，不清空服装、秘密、回访、画册、语音偏好或共同作品。
- 上次 V6 共同完成的小猫画以桌边拍立得真实出现；重新完成作品后立即更新。没有作品时不显示空相框，不把记忆变成次数、收藏值或好感度。
- 四地点支持鼠标 / 触控点击、方向键和手机横向滑动；场景图禁用浏览器原生拖图并捕获活动指针，横滑不会吞掉垂直页面滚动。减少动态模式下停止自主换帧并隐藏雨线、笔尖、布料、衣架和窗帘动作，地点、事件、天气、文字与拟音开关仍完整可用。
- 完整素材路由、衣橱提示词、拟音制作方向、导出规格与 SHA-256 见 `docs/living-room-v7-assets.md`。

### V8 房间声音与雨窗边界

- 修复连续房间的雨线覆盖室内问题：床边不显示雨线，画桌、衣橱和窗台只在各自右侧窗玻璃范围出现，宽度小于取景框的 40%，不再覆盖人物、木地板或整间房。
- 为画桌、床边、衣橱和窗台分别新增一条地点开场语音与三条事件语音；雨起和雨停另各一条，共十八条房间专属日语语音。它们全部由 `CONTENT.livingRoom.voices` 管理，不读取或复用主线 `CONTENT.voiceLines`。
- 使用 Piper Plus 0.6.0 与 `ayousanz/piper-plus-tsukuyomi-chan` 预训练模型本地合成；没有提供参考音频或说话人克隆向量。最终文件为 44.1 kHz、单声道、160 kbps MP3，并按约 -17 LUFS 统一响度。
- 将原先四段 V7 单声道拟音替换为十二段地点事件声和两段天气声；十四段均为不同的 48 kHz、立体声、192 kbps 文件。雨起 / 雨停不再借用窗台关窗或窗帘声音。
- 把“房间声”明确改名为“场景声”；它只控制物件 / 天气声。“只看角色字幕”只控制角色语音，关闭任何一方都不会连带关闭另一方。
- 新增本地 Noto Sans JP 字体子集，解决无系统日文字体环境中字幕假名显示方框的问题；日语字幕显式使用 Noto，中文视觉仍沿用原字体职责。
- 机器可读文本、模型 revision、合成参数、时长、哈希、十四段场景声和验收合同见 `docs/room-audio-v8.md` 与 `assets/audio/v8/*-manifest.json`。

### V9 音频响应与真实雨窗

- 复现旧播放器在 1.2 秒媒体等待下“点击后才发请求”的问题：房间语音约 1298ms 才进入播放状态，连续点击还会通过共享 `src` 互相中断加载。
- 为每个语音与场景声文件建立独立播放器；对应按钮或连续房间进入预备范围、房门首次打开时，以低网络优先级完整读取本地音频并转换为页面内 Blob 源。点击不再替换共享播放器地址，未准备完会立即显示状态，瞬时预热失败则重新加载仓库原文件。
- 同样 1.2 秒人为媒体等待下，预热后的点击不新增网络请求，桌面受控 Chromium 多轮实测约 65–85ms 进入播放状态；故障注入确认第一次预热失败后仍能重试播放。
- 再次按原始插画核对室外区域：画桌与共同创作只保留右上玻璃，衣橱只保留最右上窄玻璃，窗台只保留月亮所在的右上玻璃，床边完全隐藏。雨层宽度不超过取景框 19%、高度不超过 21%，不再压到人物、窗帘、窗框、灯具、桌面、床或地板。
- 桌面与手机逐项触发十八条房间角色语音和十四段场景声，确认每个点击仍启动自己的文件；两套雨景完成边界数值检查和实际截图复核。

### V10 逐窗格 Canvas 雨与重制窗雨声

- 调研 Canvas 粒子雨、按玻璃裁剪的多遮罩和高采样率室内窗雨录音的共同特征后，删除覆盖整个共同创作章节的斜纹雨背景，以及房间 / 画室两组等距 CSS 雨线。
- 依据四张运行时插画的原始像素坐标登记桌边、衣橱和窗台每一块可见玻璃；Canvas 根据 `object-fit: cover` 和 `object-position` 映射到当前取景框，逐块裁剪，窗框之间保持自然断开，床边不绘制。
- 为画桌、衣橱、窗台与共同创作设置独立场景种子；雨滴具有不同速度、长度、宽度、弧度、双层明暗拖痕、静态玻璃水珠和少量较重水珠头，30 fps 上限、设备像素比上限 2，并在离开视口、页面后台或减少动态模式时停止有效绘制。
- 重制 `weather-rain-window.mp3`：7.224 秒、48 kHz、双声道、192 kbps；使用相关立体声粉红噪声雨幕、54 颗小玻璃雨滴、13 颗重雨滴和 8 条短水流，不采用第三方录音，生成脚本与固定种子一并提交。
- 1440 × 1000 与 390 × 844 对画桌、衣橱、窗台、床边和共同创作逐项截图及非透明像素边界检查，确认无室内雨、无章节背景雨、各场景节奏不同且无页面 / 控制台错误。

### V11 玻璃多边形与真实窗雨录音

- 关闭动态 Canvas 并核对三张原始场景图，确认底图没有烘焙雨痕；问题来自 V10 的近似矩形：衣橱左上窗格在顶部压到窗帘，窗台左上窗格把人物头发遮挡区也当成玻璃，共同创作雨层还没有读取晴月状态。
- 将桌边、衣橱与窗台全部窗格改为原图像素坐标多边形，沿窗帘、人物头发、灯具与倾斜窗框保守内缩；每块路径独立裁剪，画布隐藏时仍按真实位图尺寸清空，避免床边切换回来闪出旧雨。
- 让共同创作读取同一个 `livingWeather`：细雨才绘制，晴月时房间和共同创作 Canvas 的 alpha 都归零，初始舞台标签同步为“截稿前的晴夜”。
- 用 nicoproson 在室内录制的 `RAIN on glass window.wav` 取代程序化窗雨。原作品在 Freesound 以 CC0 1.0 发布，规格为 96 kHz、32-bit、立体声；运行版本从官方 HQ 预览取 24 秒自然片段，做低频清理、-23 LUFS 归一化和短淡入淡出，最终为 24.024 秒、48 kHz、立体声、192 kbps。
- 桌面 1440 × 1000 与手机 390 × 844 逐场景进行多边形像素归属、晴 / 雨切换和截图复核；来源页、原文件哈希、许可、处理参数、运行文件哈希与复现脚本一并进入仓库。

### 桌面、手机与房门布局修复

- 修复早期桌面宽屏中区块重叠、内容挤在一起和反馈区占位不稳定的问题，让各章节回到正常文档流并保留明确阅读顺序。
- 修复手机 Chrome 切换“桌面版网站”后约 920–1024px 视口中的门页超长深蓝空白：移除与 720px 场景上限冲突的整屏最小高度，让房门区由真实内容高度决定。
- 修复桌面开门后前景门板、插画自带木门和粉色底板叠在一起的问题：在桌面断点让人物插画铺满门框、位移归零，并让前景门板在开门动画结束时完全淡出；关闭状态与原生手机构图保持不变。
- 修复五个桌面秘密“发现后标签永久展开、遮住插画”的问题：保留 44–52px 命中区，将默认标记降到几乎透明；文字只在支持悬停的设备悬停或键盘 `focus-visible` 时出现，触屏点击和已发现状态不再常驻文字。
- 每次修改 CSS 或 JavaScript 都同步递增 HTML 中的 `?v=` 缓存标识，避免 GitHub Pages、移动浏览器分别缓存新旧 HTML/CSS/JS 后造成比例、热点和门动画错位。

### 插画、五套换装与十一幕画册

- 先用身份锚点固定银白长发、蓝眼睛、粉色发饰、圆润脸型、轻 Q 比例和房间光线，再以参考编辑方式生成不同表情、服装与场景，降低角色漂移。
- 制作五套日常换装：粉色运动家居服、蓝白格深夜画稿服、水手领外出服、草莓睡前服、猫耳连帽毯；衣橱同时支持标签、上一套/下一套、键盘操作、当前状态保存和相邻图片预取。
- 将画册扩充为十一幕、三个章节：床上画画、被窝画室、安静直播、举起画稿、靠枕后递画、门缝卡片、深夜专注、整理草稿、躲在画册后、等待夸奖和抱玩偶晚安。
- 为每幕保存独立主图、缩略图、替代文本、标题、短注与角色反应；画册支持胶片缩略图、章节跳转、上一页/下一页、当前位置保存和相邻页预取。
- 增加沉浸暗房：可用方向键、按钮或手机横向滑动翻页，可放大到原图细节，包含关闭按钮、焦点循环、背景点击关闭和减少动态效果适配。
- 重做五秘密画桌场景，把耳机、轻小说稿件、数位板、猫咪玩偶与带锁抽屉固定在一张连续 4:3 插画中，并加入停留后才出现的自然文字线索与全部发现后的隐藏留言。

### 二十二句语音、字幕与情境分流

- 用 AivisSpeech Engine 1.2.0 和预训练模型“天深シノ”独立合成二十二句日语回答；没有训练、微调、克隆真实声优声纹，也没有截取动画音轨。
- 四个入口分别对应门口、画桌、夸画与晚安。十八句普通回答以轻声、普通、开心和害羞为主，各入口使用洗牌队列，一轮听完前不重复，换轮时也避免首尾紧邻重复。
- 将四句明显不高兴的 `ふきげん` 回答从普通随机池彻底移出；只有门口或画桌同一入口在 5 秒内连续触发 4 次才会播放一次，随后冷却 25 秒，下一次普通操作回到可爱语音池。
- 每个回答对象同时绑定日文、简体中文字幕、表情、场景名和本地 MP3，避免音频与字幕错配；开始新语音前会停止旧语音。
- 增加音量、停止和“只看角色字幕”控制，并保存音量与静音偏好；音频加载失败时继续显示完整字幕与视觉反应，不调用系统 TTS，也不自动播放声音。
- 为全部二十二句保存最终 MP3、完整 `audio_query`、模型与风格信息、时长和 SHA-256 清单，保证后续可核验和受控复现；模型包、引擎缓存、中间 WAV 和临时下载不进入运行时提交。

### 性能、状态与无障碍

- 将首屏表情和换装资源导出为完整尺寸与 560px 两档 WebP，通过 `srcset` 按实际屏幕选择；桌面保持高质量原始像素，窄屏避免下载过大的 PNG。
- 将约 1.5 MB 的标题 TTF 缩成当前页面字符子集 WOFF2；V8 文案同步后 ZCOOL KuaiLe 子集约 96 KB。由于原字体不含完整日文假名，另从 Noto Sans JP 的 SIL OFL 1.1 字体制作约 104 KB 当前字符子集，并让日语字幕优先使用；页面不再依赖访客设备恰好安装日文字体。画册、衣橱和连续房间只预取当前相邻或即将使用的内容。
- 在同一 390px 冷缓存条件下，页面请求量从约 10.79 MB 降到约 1.04 MB，减少约 90.4%；运行时不热链第三方图片、字体或音频。
- 将持久状态集中到 `sagiri-room-state-v2`，保存服装、五秘密、回访、共同完成的画、连续房间位置与天气、场景声开关、画册位置、音量和只看角色字幕偏好，并保留从 v1 迁移服装与秘密记录的兼容逻辑。
- 补齐按钮语义、可见键盘焦点、图片替代文本、语音字幕、暗房焦点管理和 `prefers-reduced-motion`；禁用 JavaScript 时仍保留可读的基础介绍与素材说明。

### 文档、许可与协作治理

- 建立 `PRODUCT.md` 作为产品定位与体验边界，建立 `DESIGN.md` 作为颜色、字体、组件、响应式、动效和无障碍的现役规范。
- 建立本手册，记录 AI 协作范围、语音模型官方来源、本地合成方式、许可边界、旧版 v3 复现资料、验证清单和路线图。
- 分别保存五秘密画桌、八张新增害羞画册、两套新增换装、V6 雨夜共同创作、V7 连续房间素材和 V8 房间声音的提示词、参考素材、合成方法、导出规格与校验值，方便后续在不破坏角色一致性的前提下维护。
- 建立 `AGENTS.md`，固定零框架架构、本地资源、UTF-8、集中内容对象、最低验证、文档同步和“完成后默认提交、推送、合并、上线并在线复核”的协作规则。
- 在页脚和 README 保留 AivisSpeech“天深シノ”、Piper Plus“つくよみちゃん”模型来源 / 条款链接，以及站酷快乐体、Noto Sans JP 与 SIL Open Font License 1.1 的说明，并明确页面不代表版权方官方立场。

### 发布提交与对应结果

| 日期 | 提交 | 已完成结果 |
| --- | --- | --- |
| 2026-08-09 | [`27b12af`](https://github.com/xinchenok/izumi-sagiri-room/commit/27b12afeb2ba2879caddf73fc4a59d632b0b8495) | 首次发布完整互动角色页、基础图片与语音、产品说明、设计系统及 GitHub Pages 配置。 |
| 2026-08-10 | [`f096392`](https://github.com/xinchenok/izumi-sagiri-room/commit/f096392b1724406686c0cbcf41830c538cc4bee8) | 新增 AI 协作与语音复现手册、项目协作规则和 README 文档入口。 |
| 2026-08-11 | [`8b65c2a`](https://github.com/xinchenok/izumi-sagiri-room/commit/8b65c2a136d1e153089ac9d1ce93228bce1488af) | 修复桌面布局，重做五秘密画桌，补充旧版语音语气资源与复现说明。 |
| 2026-08-11 | [`41452b3`](https://github.com/xinchenok/izumi-sagiri-room/commit/41452b3c20bf489864b5ec272bdfd5e4d9e80649) | 修复移动端缓存与布局，建立 v4 独立语音清单，扩充七幕害羞画册并同步发布规则。 |
| 2026-08-11 | [`a2771c7`](https://github.com/xinchenok/izumi-sagiri-room/commit/a2771c713f15c57a4b1a5140c0fe9ca0a9d82646) | 消除门页超长空白与秘密标签遮挡，再增四幕可爱害羞画册、六句轻软语音和连续打扰才触发的不高兴分流。 |
| 2026-08-11 | [`c608216`](https://github.com/xinchenok/izumi-sagiri-room/commit/c608216220abcaaec737d722161262549c65cae3) | 修复约 980px 桌面站点模式开门后的双门叠加、粉色竖条、残缺门牌和边框错位。 |
| 2026-08-11 | [`27f80fc`](https://github.com/xinchenok/izumi-sagiri-room/commit/27f80fcf2c8dc82be9a2aed34186775cc22a7bcb) | 加入三阶段回访、五套换装、三章画册与暗房、语音控制、响应式 v5 图片、字体子集和按需预取，完成当前用户可见功能基线。 |
| 2026-08-13 | [`5cf713a`](https://github.com/xinchenok/izumi-sagiri-room/commit/5cf713a9e08edc720e8463e0011de76acee39257) | 新增“今晚，陪她画完这一张”共同创作、四帧雨夜舞台、三张可选成稿、具体回访记忆、V6 字体子集与响应式 / 减少动态适配。 |
| 2026-08-13 | [`fe27d6f`](https://github.com/xinchenok/izumi-sagiri-room/commit/fe27d6f6f15bfcdda284abcbae82c69d2e063a9f) | 通过 PR #2 发布 V7“会生活的房间”：四个连续地点、自主细节、重复小事件、独立短拟音、共同作品陈列、旧状态兼容与响应式 / 减少动态适配。 |
| 2026-08-13 | [`b805201`](https://github.com/xinchenok/izumi-sagiri-room/commit/b8052011a1fad506efd2fd1d029e6d5a36ebc3d1) | 通过 PR #4 发布 V8 房间声音：十八条地点 / 事件 / 天气专属配音、十四段不复用场景声、互相独立的声音控制、窗玻璃雨线边界与完整日语字幕字体。 |
| 2026-08-13 | [`5af5f9b`](https://github.com/xinchenok/izumi-sagiri-room/commit/5af5f9b7a122f168030191cab1ea36d12fa82deb) | 通过 PR #6 发布 V9 音频响应与雨线修复：逐文件播放器和 Blob 预热消除点击换源延迟，失败可重试，房间层与插画层雨线均限制在真实玻璃范围。 |
| 2026-08-14 | [`d1a08d9`](https://github.com/xinchenok/izumi-sagiri-room/commit/d1a08d949cd6545ca8c84a9b42886ade29bcb599) | 通过 PR #7 发布 V10 逐窗格雨景：删除室内斜纹和固定 CSS 雨线，按真实玻璃裁剪独立 Canvas 雨滴，并重制可复现的 7.224 秒立体声窗雨。 |

### 已完成的交付核验

- V6 知识收尾开始前，本地功能分支与当时远端 `main` 的完整 Git tree 一致，156 个受版本控制文件全部存在且非空，没有漏传或未提交的运行时代码、图片、字体、音频、查询 JSON 和提示词文档。
- V6 上线后正式网址返回 `HTTP 200`，线上 HTML 使用 `styles.css?v=20260813-1` 与 `script.js?v=20260813-1`；当时线上 HTML、CSS 和 JavaScript 与 V6 功能基线逐字节一致。
- 受控 Chromium 已覆盖 2048 × 1138、1440 × 900、1024 × 768、约 980px 桌面站点模式和 390 × 844；检查了开/关门构图、横向溢出、五个热点、五套换装、十一幕画册、暗房、语音分流与降级、回访状态和减少动态效果。
- V6 共同创作额外在 1440 × 900、1024 × 768 与 390 × 844 完成真实点击、键盘 Enter、返回上一步、完成后焦点、本地记忆回显、无横向溢出和无控制台 / 页面 / 资源错误检查；81 种陪伴 × 主题 × 配色 × 感想组合全部生成正确成稿和状态。390px 的减少动态模式已确认雨线与笔尖微光隐藏、随机眨眼停止。
- V7 在 1440 × 1000、1024 × 900 与 390 × 844 完成四地点切换、四地点重复小事件、约九秒自主变化、桌边成稿陈列、方向键、手机横滑、房间天气、独立拟音开关和刷新持久化检查；三档均无横向溢出、404、控制台或脚本错误。旧 V6 状态缺失三个新字段时安全回落；390px 减少动态模式确认雨线与四类物件动作隐藏、过渡为 0.01ms。额外故障注入确认衣橱图片加载失败时，地点文字、深层入口和持久状态仍会切换；进入房间时只预取相邻场景，不提前下载衣橱新图。
- V7 发布树共有 178 个受版本控制文件，全部存在且非空；静态门禁包括 `node --check script.js`、CSS 语法、全部本地资源引用、93 张 PNG / WebP 解码、42 段 MP3 / WAV 解码、二十二句语音清单哈希与时长、七项 V7 新资源哈希、标题字体字符覆盖、UTF-8 无 BOM、乱码特征、Markdown 内链、键盘操作和 `20260813-2` 缓存标识。
- PR #2 的远端功能树 `aff11ec9f410997137f08179eff7ea59ce481153` 与本地验收树一致并合并为 `fe27d6f`；正式 Pages 返回 `HTTP 200` 和 `20260813-2`，线上 `index.html`、`styles.css`、`script.js`、字体、三张 V7 衣橱图片与四段 V7 拟音共 11 个关键运行文件均与本地版本 SHA-256 逐字节一致，V7 状态为 `live verified`。
- V8 发布树共有 215 个受版本控制文件；静态门禁包括 JS / CSS / JSON、114 个本地资源引用、Markdown 内链、UTF-8 与重复 ID，93 张图片和 74 段音频全部解码。十八条房间配音与十四段场景声逐项核对唯一 ID、唯一文件、清单 SHA-256 和时长，日语字幕 173 个实际字符均由本地字体覆盖。Playwright 在桌面、1024px、390px、减少动态和 V6 旧状态下通过，并逐项触发 4 个地点开场、12 个地点事件、2 个天气状态、14 段场景声、两套独立静音与声音失败降级。
- PR #4 的远端功能树 `f1159e7404b527034208e9ff95dfdf658e236d20` 与本地验收树一致并合并为 `b805201`；正式 Pages 返回 `HTTP 200` 和 `20260813-3`，线上页面三件套、两份音频清单、两套本地字体及全部 V8 配音 / 场景声等 40 个关键运行文件均与本地版本逐字节一致，V8 状态为 `live verified`。

## 当前项目结构

| 路径 | 用途 |
| --- | --- |
| `index.html` | 页面语义结构、无脚本降级内容和素材署名 |
| `styles.css` | 房间视觉、响应式布局、动效与减少动态效果适配 |
| `script.js` | 集中内容数据、交互、音频、状态保存与 v1 → v2 迁移 |
| `assets/v3/` | 原始人物 / 换装 PNG 维护源与五秘密画桌资源 |
| `assets/v4/` | 十一张统一 4:3 的运行时画册 WebP 与十一张 480 × 360 缩略图 |
| `assets/v5/` | 四张人物表情与五套换装的完整尺寸 / 560px WebP，以及两套新增换装生成源 PNG |
| `assets/v6/` | 四组共同创作人物帧的 1448 × 1086 / 720 × 540 WebP，以及三张 960 × 720 小猫成稿 |
| `assets/v7/` | V7 衣橱场景的 1448 × 1086 / 720 × 540 WebP 与生成源 PNG |
| `assets/audio/v4/` | 二十二句不同回答的 MP3、完整 `audio_query` 与机器可读合成清单 |
| `assets/audio/v7/` | V7 四段旧拟音的历史复现资源；当前页面不再读取 |
| `assets/audio/v8/` | 十八条房间专属配音、十四段独立场景声、两个机器可读清单与真实窗雨来源合同 |
| `assets/fonts/` | ZCOOL KuaiLe 与 Noto Sans JP 当前页面字符子集、维护源和字体许可 |
| `PRODUCT.md` | 产品定位、设计原则与无障碍目标 |
| `DESIGN.md` | 视觉规范、组件状态、响应式和动效规则 |
| `docs/ai-assisted-development.md` | 本文：AI 协作过程、语音复现和路线图 |
| `docs/desk-secrets-image-prompt.md` | 五秘密画桌的完整生图提示词、参考图角色与素材校验值 |
| `docs/gallery-v4-image-prompts.md` | 八张新增害羞画册场景的完整提示词、参考图与导出规格 |
| `docs/outfit-v5-image-prompts.md` | 两套新增换装的完整提示词、参考图、校验值与高质量 WebP 导出规格 |
| `docs/drawing-v6-image-prompts.md` | V6 共同创作舞台与三张成稿的生成方向、导出规格和 SHA-256 |
| `docs/living-room-v7-assets.md` | V7 场景复用、衣橱提示词、拟音方法、导出规格和 SHA-256 |
| `docs/room-audio-v8.md` | 当前房间配音、场景声、雨窗边界、许可与验收合同 |

网站是零框架、无后端的静态页面。浏览网站时不需要 AivisSpeech、Piper Plus、语音模型或任何外部运行时服务；它们只在重新生成音频时需要。

## 角色插画的制作方式

本项目没有使用官方截图。插画工作流如下：

1. 先生成身份锚点，确定银白长发、蓝眼睛、粉色发饰、圆润脸型和约 5.5 头身的轻 Q 比例。
2. 以身份锚点为编辑参考，锁定脸部、发型、比例和房间光线，再只改变表情、服装或场景。
3. 首屏生成安静偷看、突然受惊、害羞微笑和认真得意四种表情。
4. 换装先生成宽松粉色运动家居服、蓝白格深夜画稿睡衣、水手领裙装与薄荷针织外套三种明显不同的轮廓，再补充“侧躺床上仍在画画的草莓睡前服”和“雨夜裹住自己校稿的猫耳连帽毯”。
5. 画廊在原有“深夜专注、躲在画册后、抱玩偶晚安”之外，先补充“床上画画、举起画稿、躲在靠枕后递画、装作没等夸奖”，再补充“被窝画室、安静直播、门缝递卡片、整理草稿”四种情景；运行时按十一幕小故事排列，不再用换装图填充画廊。
6. 五秘密画桌以首屏身份图、深夜画稿图和家居服图作为参考，明确固定耳机、轻小说稿件、数位板、猫咪玩偶和带锁抽屉五个互不遮挡的点击区域。
7. 生成时明确要求全年龄、非性感化、无图片内文字、无水印、不复刻官方截图。

旧版 PNG 保留在仓库中作为维护源；首屏与换装运行时使用 `assets/v5/`，画册使用 `assets/v4/`，五秘密画桌继续优先使用 `assets/v3/desk-secrets.webp`，共同创作使用 `assets/v6/`，连续房间复用合适的 v4 / v6 场景并以 `assets/v7/wardrobe-living.webp` 补齐衣橱。新增画册、换装、共同创作和连续房间的完整提示词、校验值与导出方法分别见 `docs/gallery-v4-image-prompts.md`、`docs/outfit-v5-image-prompts.md`、`docs/drawing-v6-image-prompts.md` 和 `docs/living-room-v7-assets.md`。

## 语音模型的来源与下载位置

### 官方来源

- AivisSpeech 下载：<https://aivis-project.com/AivisSpeech>
- “天深シノ”模型页：<https://hub.aivis-project.com/aivm-models/0f6821f4-9f86-4da1-a41a-fbe6fff9ca88>
- AivisSpeech Engine 源码与版本：<https://github.com/Aivis-Project/AivisSpeech-Engine>

请从上述官方页面安装，不要从来历不明的网盘或二次打包站下载。

### 本项目使用的模型信息

| 项目 | 内容 |
| --- | --- |
| 模型名称 | 天深シノ |
| 模型 UUID | `0f6821f4-9f86-4da1-a41a-fbe6fff9ca88` |
| 许可 | ACML-NC 1.0，限许可允许的非商业用途 |
| 语言 | 日语 |
| 架构 | Style-Bert-VITS2（JP-Extra），ONNX |
| 模型包 | AIVMX，模型页标示约 258.28 MB |
| SHA-256 | `89bb3b704054ef0f08e91fe33ea30f866f14913921bcebfd11e2609bf2b721de` |

许可和模型版本可能更新；重新下载时应以模型页当时展示的信息为准，并再次确认使用场景是否仍符合许可。

### 安装位置

在 AivisSpeech 运行时打开模型页，可以使用页面提供的一键安装。Windows 手动放置模型时，AivisSpeech 官方说明的目录是：

```text
C:\Users\<用户名>\AppData\Roaming\AivisSpeech-Engine\Models
```

macOS 对应目录是：

```text
/Users/<用户名>/Library/Application Support/AivisSpeech-Engine/Models
```

本项目曾使用的临时下载目录和引擎缓存已在音频生成后清理，因此仓库中不包含约数百 MB 的模型包，也不包含 AivisSpeech Engine。需要重新生成时，请从官方来源重新安装。

## 当前二十二句不同回答如何生成

### 角色化写法

四类互动分别是房门口、画桌旁、被夸以后与晚安以前。普通池共有十八句独立日文文本，以“轻声、短句、害羞但会一点点放松”为主；涉及画稿时更认真，被夸时会先确认是不是客套。另外四句 `ふきげん` 回答属于连续催门或催稿的情境反馈，不进入普通随机。它表达的是本页角色设定与情境，不照录动画台词，也不冒充真实声优。

运行时数据保存在 `script.js` 的 `CONTENT.voiceLines`。`chooseVoiceReply()` 为每个普通入口维护一份洗牌队列：当前入口的普通回答全部弹出前不会重复；重新洗牌时若队首会与上一轮末句相同，会先交换位置。`shouldUseGuardedReply()` 只在同一入口 5 秒内触发 4 次时放行情境池，并设置 25 秒冷却；这四句不会被 `chooseVoiceReply()` 选中。字幕、表情、反应文字和音频路径都来自同一个回答对象，因此不会出现“声音换了但字幕没换”的错配。

### 合成与可复现文件

- AivisSpeech Engine：1.2.0。
- 同一模型：天深シノ；模型 UUID、说话人 UUID、模型包 SHA-256、风格 ID、每句文本、参数、时长与输出 SHA-256 全部保存在 `assets/audio/v4/voice-manifest.json`。
- 根据句意使用同一模型提供的 `ささやき`、`ノーマル`、`じょうきげん` 或 `ふきげん` 表达；普通随机池不含 `ふきげん`，`pitchScale` 始终为 0，没有用后期移调制造不同音色。
- 每句由 `POST /audio_query` 和 `POST /synthesis` 独立生成；完整查询响应保存在 `assets/audio/v4/queries/<line-id>.json`。
- 最终网页资源统一为 44.1 kHz、单声道、112 kbps MP3。中间 WAV 只用于编码，不提交模型包、引擎或临时缓存。

重新合成时，先按下方官方来源安装引擎与模型，再用 `GET /speakers` 核对当前风格 ID；复用对应查询 JSON 调用 `/synthesis`，并用清单里的 SHA-256、时长、采样率和声道数验收。复合风格 ID 可能随安装变化，必须按模型名称、模型 UUID 与风格名称重新解析，不能盲信旧数字。

## 旧版 v3：四段台词与十二种语气

以下内容仅保留为旧版资源的历史复现记录；当前页面不再读取 `assets/audio/v3`，也不再用同一句的轻度后期版本轮换。

### 台词、风格与输出文件

| 场景 | 日语台词 | 模型风格 | 本次本地引擎 ID | 输出文件 |
| --- | --- | --- | --- | --- |
| 房门口 | お、おかえり……今日も、静かにしてね。 | ささやき／耳语 | `1063997411` | `assets/audio/v3/welcome.wav` |
| 画桌旁 | まだ描き終わってないから……勝手に見ちゃだめ。 | ふきげん／不高兴 | `1063997410` | `assets/audio/v3/drawing.wav` |
| 衣橱相册 | 気に入ったなら……もう一枚、描いてあげてもいいよ。 | じょうきげん／开心 | `1063997409` | `assets/audio/v3/like.wav` |
| 晚安以前 | おやすみ。明日も……一緒に頑張ろうね。 | ノーマル／普通 | `1063997408` | `assets/audio/v3/goodnight.wav` |

模型页中四个风格的模型内 ID 分别为 `3`、`2`、`1`、`0`；表格里的十位数 ID 是本次 AivisSpeech Engine 返回的本地复合风格 ID。重新安装模型后，本地 ID 可能变化，因此不要把这些数字当成永久接口。

旧版 v3 WAV 可用下面的 SHA-256 校验：

| 文件 | SHA-256 |
| --- | --- |
| `welcome.wav` | `0cf48ccbd6240956086a13b4f81354006a6ba392f3aae13c76aa88aca0ae9fad` |
| `drawing.wav` | `8b503fdb5249f85233a998d392b95f7179559b134214a35e0f5041261cd1e7f0` |
| `like.wav` | `dd8562efea4cf32b8fb6bec059b91c781499fc94d8306d17f3dfdd558d196b6a` |
| `goodnight.wav` | `d1280b58d1f57d2e4f8b32777cb4650f0788c591ce046416239e7842b8c38859` |

### 轻度后期语气版本

八段 MP3 都直接来自对应原始 WAV，只调整少量音高、速度、音量和高低通，不新增或替换任何字词。页面仍展示与原始台词逐字一致的日文和中文字幕。编码统一为 44.1 kHz、单声道、112 kbps MP3；制作时使用 FFmpeg 6.1.1。

| 场景 | 页面语气 | 输出文件 | 处理参数摘要 |
| --- | --- | --- | --- |
| 房门口 | 更小声 | `welcome-soft.mp3` | 音高 +1.5%、速度放慢约 3%、音量 86%、70 Hz–11.5 kHz |
| 房门口 | 慌张一点 | `welcome-flustered.mp3` | 音高 +4.5%、整体稍快、音量 92%、70 Hz 高通 |
| 画桌旁 | 小声嘟囔 | `drawing-mutter.mp3` | 音高 -0.5%、速度放慢约 2.5%、音量 82%、70 Hz–10.5 kHz |
| 画桌旁 | 被抓到啦 | `drawing-flustered.mp3` | 音高 +3.5%、整体稍快、音量 90%、70 Hz 高通 |
| 衣橱相册 | 偷偷开心 | `like-soft.mp3` | 音高 +1.5%、速度放慢约 3%、音量 86%、70 Hz–11.5 kHz |
| 衣橱相册 | 藏不住开心 | `like-happy.mp3` | 音高 +4.2%、整体稍快、音量 94%、70 Hz 高通 |
| 晚安以前 | 更轻一点 | `goodnight-soft.mp3` | 音高 +0.8%、速度放慢约 4%、音量 80%、65 Hz–9.8 kHz |
| 晚安以前 | 困困的 | `goodnight-sleepy.mp3` | 音高 -1.2%、速度放慢约 6%、音量 78%、65 Hz–9 kHz |

旧版 v3 MP3 可用下面的 SHA-256 校验：

| 文件 | SHA-256 |
| --- | --- |
| `welcome-soft.mp3` | `522c4f2ac1c39888ae9a1b7ab58839eab095c6c4959672d249ea5ffb5fbea70a` |
| `welcome-flustered.mp3` | `8001d09de71ba0c20ccb51eeca3578c38daef23344f22c3627471359dba7ccad` |
| `drawing-mutter.mp3` | `48b92d99667bcfa6c7567ad8cee9f2274068f323316db4e21a846eb0761511e4` |
| `drawing-flustered.mp3` | `03a72c874df16fd32a24118b5ee7c00f3eaea6990a17d0672a2fe3009e498190` |
| `like-soft.mp3` | `f3ab735e3b68f9d38e857f89e15e99c29b5dbcec98a1b06a4ef228a89468b908` |
| `like-happy.mp3` | `cfa54abd771f02e5bb15c6c93099df1b91c3856b72e96d1f41d9367e94e6759e` |
| `goodnight-soft.mp3` | `1c428767b4d0066b6116ea8fcbb313dd381ec4a3288d01e0423c557b395b4a62` |
| `goodnight-sleepy.mp3` | `6b8263d7ea2c701346fa459f74a7991cae4b9ed8be0b55095d6cb7a93259041d` |

### 本地合成流程

1. 安装并启动 AivisSpeech，让本地 Engine 正常运行。
2. 安装“天深シノ”模型。
3. 请求 `GET /speakers`，根据模型 UUID、说话人名称和风格名称读取当前安装实际返回的 `styles[].id`。
4. 对每句文本调用 `POST /audio_query` 生成查询参数。
5. 将查询 JSON 传给 `POST /synthesis`，保存返回的 WAV。
6. 把 WAV 放入 `assets/audio/v3/`，并核对 `script.js` 中的文件路径、日文台词、中文字幕和表情映射。

AivisSpeech Engine 默认提供与 VOICEVOX 兼容的本地 HTTP API。下面是 PowerShell 示例；执行前必须先从 `/speakers` 核对当前风格 ID：

```powershell
$engineBase = "http://127.0.0.1:10101"

# 先查看当前安装实际提供的说话人与风格 ID。
$speakers = Invoke-RestMethod -Method Get -Uri "$engineBase/speakers"
$speakers | ForEach-Object {
  $speaker = $_
  $speaker.styles | ForEach-Object {
    [PSCustomObject]@{
      Speaker = $speaker.name
      Style = $_.name
      Id = $_.id
    }
  }
} | Format-Table

# 用刚刚核对出的 ID 生成一条语音。
$lineText = "お、おかえり……今日も、静かにしてね。"
$styleId = 1063997411
$escapedText = [Uri]::EscapeDataString($lineText)
$queryUri = "$engineBase/audio_query?text=$escapedText&speaker=$styleId"
$query = Invoke-RestMethod -Method Post -Uri $queryUri
$queryJson = $query | ConvertTo-Json -Depth 100 -Compress
$queryBytes = [Text.Encoding]::UTF8.GetBytes($queryJson)

Invoke-WebRequest `
  -Method Post `
  -Uri "$engineBase/synthesis?speaker=$styleId" `
  -ContentType "application/json" `
  -Body $queryBytes `
  -OutFile "assets\audio\v3\welcome.wav"
```

旧版的四个原始文件为 44.1 kHz、单声道 WAV。v3 页面曾从该场景的原始 WAV 和两段后期 MP3 中随机选择；当前 v4 页面已经改为二十二句独立回答。开始新回答前仍会停止上一段；播放失败时保留日文、简体中文字幕和视觉反馈，且不会回退到系统 TTS。

以上记录了旧版 WAV 校验值；v4 已为全部二十二句保存完整 `audio_query`、引擎版本和机器可读清单。

## 许可与伦理边界

- 不使用动画原声、官方截图或未经授权的角色声纹数据。
- 不训练、微调或分发藤田茜或其他真实声优的声纹克隆模型。
- 不把“风格相近”描述为“原作声音”或“官方配音”。
- 不在仓库中重新分发“天深シノ”模型包；仓库只保存页面需要的最终 MP3、查询 JSON 与合成清单。
- 当前语音模型采用 ACML-NC 1.0，网站必须维持许可允许的非商业使用范围并保留来源说明。
- 不在仓库中重新分发 Piper Plus“つくよみちゃん”模型；仓库只保存十八段最终 MP3 与可复现清单，并长期保留模型卡和つくよみちゃんコーパス条款链接。
- Noto Sans JP 子集与许可文本一起保存在 `assets/fonts`，继续按 SIL Open Font License 1.1 使用。
- 若未来出现商业化、广告、付费下载或品牌合作，必须先停用现有语音并重新完成角色版权、模型许可和字体许可审查。

## 验证与上线方式

旧有全站基线已在受控 Chromium 中完成 2048 × 1138、1440 × 900、1024 × 768 与 390 × 844 四档回归。2026-08-13 对 V6 共同创作追加 1440 × 900、1024 × 768 与 390 × 844 三档回归：真实点击、键盘 Enter、返回、完成焦点、刷新后的作品记忆、减少动态效果、81 种分支组合和横向溢出均通过，本站控制台、页面与本地资源请求没有错误。

V5 同一 390px 冷缓存测量中，页面请求量由约 10.79 MB 降至约 1.04 MB，减少约 90.4%。V6 没有把新的四帧舞台和三张成稿塞进首屏：专注帧随章节懒加载，眨眼帧只在章节进入视口后预取，害羞 / 完成帧只在开始互动后预取，成稿只在选择主题后预取。原始像素尺寸没有下降：桌面主图仍为 1122 × 1402，窄屏按实际显示尺寸选择 560 × 700 版本；共同创作舞台使用 1448 × 1086 / 720 × 540 两档。标题字体从约 1.5 MB 原始 TTF 改为约 89 KB 当前字符子集 WOFF2。

发布前仍应复核下列完整清单：

- 2048×1138、1440×900、1024×768 和 390×844 四种视口的布局与横向溢出检查。
- 门板键盘操作、五套换装、十一张画廊内容、三章与暗房、五个彩蛋、渐进线索、隐藏留言和 v1 状态迁移。
- 四个主线入口、十八句普通回答的洗牌轮换、四句情境回答的连续点击阈值；房间 4 条地点、12 条事件、2 条天气语音全部独立；跨场景打断和缺失文件降级。
- 十二段地点场景声与两段天气场景声全部独立；“场景声”和“只看角色字幕”互不连带。
- `prefers-reduced-motion` 下关闭持续动画、视差和纸屑。
- JavaScript 禁用时的基础内容可读性。
- 控制台错误、页面错误和资源 404 检查。
- Node.js 语法检查、UTF-8 无 BOM 检查和中文乱码特征扫描。

站点目前由 GitHub Pages 从 `main` 分支根目录发布。普通内容更新的维护流程是：

1. 在功能分支修改并本地验证。
2. 提交并推送到 GitHub。
3. 合并到 `main`。
4. 等待 GitHub Pages 构建完成。
5. 打开在线地址检查标题、主要交互、音频和静态资源是否正常。

修改 `styles.css` 或 `script.js` 时，必须同时递增 `index.html` 中对应 URL 的 `?v=` 发布标识。GitHub Pages 与移动浏览器可能分别缓存 HTML、CSS 和 JavaScript；版本标识可避免新 HTML 搭配旧样式或旧脚本，造成图片比例、热点位置和互动结构错乱。

## 已知问题与后续规划

### P0：自动检查

- 增加 GitHub Actions：检查 `script.js` 语法、静态资源引用、UTF-8 无 BOM、乱码特征和 GitHub Pages 冒烟访问。
- 在真实低端 Android 与慢速移动网络继续记录 LCP / INP；本地容器数字只用于同条件前后对比，不冒充真实用户监控。

### P1：语音无障碍与自动复现

- 完成屏幕阅读器对音量、停止、只看角色字幕、场景声和暗房焦点循环的复测；保持不加入暂停与机械式进度条。
- 可增加一条读取 `voice-manifest.json` 与查询 JSON 的受控本地生成脚本；仍要求操作者自行从官方来源安装模型，不在仓库中分发模型。

### P2：内容扩展

- 在不改变全年龄与非性感化边界的前提下增加季节换装和新画稿。
- 若要加入离线访问，可评估 PWA；若要使用自定义域名，再补充域名、缓存与回滚说明。
- 每次更换图像、字体或语音模型时重新审查来源、许可和页脚署名。

## 维护者检查清单

每次发布前至少确认：

- [ ] 新素材有明确来源、许可和用途边界。
- [ ] 没有提交模型缓存、临时下载、个人令牌或本机绝对路径。
- [ ] `script.js` 的集中数据与页面文案一致。
- [ ] 音频由用户操作触发，字幕始终可见，失败时不调用系统 TTS。
- [ ] 键盘焦点、颜色对比、减少动态效果和无脚本内容仍然有效。
- [ ] 桌面、平板、手机均无横向溢出，在线资源没有 404。
- [ ] 所有源码、Markdown 和配置均为 UTF-8 且无 BOM，中文无乱码。
- [ ] `README.md`、`PRODUCT.md`、`DESIGN.md` 与本文仍和实际代码一致。
