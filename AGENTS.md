# 项目协作规则

## 环境与编码

- 所有命令默认使用 PowerShell，路径使用 Windows 格式。
- 文本搜索使用 `Select-String`，文件查看使用 `Get-ChildItem`。
- 所有源码、Markdown、配置和数据文件统一使用 UTF-8，不允许 BOM。
- 中文注释、日志、错误信息和文档统一使用简体中文。
- 发现疑似乱码时，必须先使用 `Get-Content -Encoding UTF8` 读取，并执行乱码特征扫描。

## 稳定架构约束

- 项目保持零框架、无后端、无运行时外部服务，直接打开 `index.html` 可读可用。
- 人物、服装、语音、画廊和彩蛋内容集中维护在 `script.js` 的 `CONTENT` 对象中。
- 浏览器持久状态使用 `sagiri-room-state-v2`；修改结构时保留必要的旧版本迁移。
- 运行时素材必须保存在仓库本地，禁止热链官方素材或第三方临时地址。
- 不提交动画原声、官方截图、声优声纹克隆、语音模型包或本地模型缓存。
- 当前语音模型仅用于许可允许的非商业展示；更换模型或用途前重新审查许可。
- GitHub Pages 从 `main` 分支根目录发布。

## 文档入口

| 文档 | 内容 |
| --- | --- |
| `README.md` | 项目入口、本地浏览和功能概览 |
| `PRODUCT.md` | 产品定位、原则与无障碍目标 |
| `DESIGN.md` | 视觉、组件、响应式和动效规范 |
| `docs/ai-assisted-development.md` | AI 协作过程、语音复现、上线与路线图 |

改动功能、设计、素材、语音或部署方式时，同步更新对应文档；不要在本文件记录逐次改动历史。

## 最低验证要求

- 执行 `node --check script.js`。
- 检查页面引用的本地资源均存在且无 404。
- 检查桌面、平板和手机布局，没有横向溢出。
- 检查键盘焦点、字幕、音频失败降级和 `prefers-reduced-motion`。
- 使用 UTF-8 无 BOM 检查和乱码特征扫描验证全部文本文件。

## 发布约定

- 用户未明确要求“仅保留本地”或“暂缓上线”时，完成改动并通过最低验证后，默认提交到功能分支、推送并合并到 `main`，由 GitHub Pages 上线；不再停在本地等待二次确认。
- 发布后必须核对目标提交已进入远端 `main`、Pages 部署成功，并通过正式网址验证用户可见版本；未闭环的步骤必须明确标为待处理。
